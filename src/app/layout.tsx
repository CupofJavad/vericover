import type { Metadata } from "next";
import { DM_Sans, Instrument_Serif } from "next/font/google";
import "./globals.css";

const dmSans = DM_Sans({
  variable: "--font-dm-sans",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

const instrumentSerif = Instrument_Serif({
  variable: "--font-instrument-serif",
  subsets: ["latin"],
  weight: ["400"],
});

export const metadata: Metadata = {
  title: "VeriCover — On-Chain Parametric Cover",
  description:
    "Immutable policy NFTs, transparent USDC reserve pools, and automated parametric claims on Base. The financial layer of insurance, fully verifiable on-chain.",
  openGraph: {
    title: "VeriCover — On-Chain Parametric Cover",
    description:
      "Purchase cover, stake capital, and verify every policy lifecycle on-chain.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${dmSans.variable} ${instrumentSerif.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}